# 09 エンジニア

← [[08_画面操作オペレーター]] | [[00_CEO_DASHBOARD|CEOへ戻る]]

最終更新: 2026-08-03

---

## 担当業務

n8nワークフローJSON・Skill・Credentialアーキテクチャの設計と実装。

## 行動規範（詳細は `.claude/agents/engineer.md`）

- 既存の実装パターン（Anthropicクレデンシャル参照方法、graceful fallback設計、qa_status=PASSゲート）に準拠する
- APIキー・Secretをコードに直書きしない
- 自動投稿・自動送信ロジックは「下書き準備＋停止」をデフォルトにする
- `git add .` を使わず個別ファイル指定でコミットする

---

## 既知の技術的負債・ハマりポイント

| 項目 | 内容 |
|---|---|
| n8n再インポート時のCredential未紐付け | 新規ワークフローJSONをインポートすると、Anthropic/Bearer Auth等のCredential参照が外れることがある。再インポート後は必ずノードを開いて紐付けを確認する |
| フィールド不整合 | パイプライン段間（101→102→103等）でフィールド名の不一致が起きやすい。REPORT-014で修正実績あり |
| max_tokens不足 | DEV_RIO_103で初回実データ実行時に発覚（REPORT-015で修正） |
| httpRequestのクエリパラメータに複数行テキストを渡すと400 | Threads API等で改行・URL・特殊文字入りテキストを `sendQuery` で送ると「Bad request」になるケースがある（DEV_RIO_705本番テストで発覚）。POST パラメータは必ず Code ノードで body オブジェクトを組み立て `specifyBody: json` + `jsonBody={{ $json.xxx_body }}` で送ること（Anthropic呼び出しと同じパターン） |

---

## 主要Credential一覧（値は書かない・IDのみ）

| Credential名 | 用途 | ID |
|---|---|---|
| Anthropic account | Claude API呼び出し全般 | gSjvXXaj0OLWBIza |
| Bearer Auth account | Threads API | EqL89RW6m6CtCIbm |
| LINE Messaging API | LINE公式アカウント通知 | （n8n Credentials参照） |

---

## 直近の作業ログ

| 日付 | 内容 | 成果物 / 参照 |
|---|---|---|
| 2026-08-02 | DEV_RIO_705をdry-runから実Threads API連携版に更新 | REPORT-029, PR #42 |
| 2026-08-02 | AI社員10名分のエージェント定義（.claude/agents/）+ Obsidian vault構築 | このセッション |
| 2026-08-02 | DEV_RIO_705 Threads API「Bad request」対策。本番テストで2件中1件が400失敗（改行・URL・特殊文字入り `final_reply` をクエリパラメータで送信していたのが原因と推定）。Create Threads Container / Publish Threads Post を **クエリ送信 → JSONボディ送信**（`specifyBody: json` + `jsonBody={{ $json.threads_body / $json.publish_body }}`、Anthropic呼び出しと同一パターン）に変更。`threads_body` は Match Rakuten Link 内で組み立て、`publish_body` は新設の Build Publish Body Codeノード（コンテナ作成失敗時は creation_id='' → 従来通り POST_FAILED 集計）で組み立て。Link Gate / NO_LINK_SKIP / 【PR】表記 / Bearer Auth credential (EqL89RW6m6CtCIbm) は不変。JSON構文検証＋Node.jsシミュレーション（/tmp/sim_705_body.js・20項目）ALL PASS。**注意: n8nへ再インポート必要。再インポート後は Anthropic / Bearer Auth の Credential 紐付けを必ず確認。** 未コミット（CEOがまとめる） | このセッション |
| 2026-08-02 | 楽天リンクライブラリ導入＋DEV_RIO_705改修。`data/rakuten_link_library.csv` 新規作成（ゆうさん発行の実リンクのみ登録する台帳・初期は空）。705に Link Library（定数）/ Match Rakuten Link / Link Gate ノードを追加し、genre一致の実リンクがある下書きだけ【PR】表記＋実リンク付きで投稿、一致しなければ `NO_LINK_SKIP` で投稿しない設計に変更。ライブラリが空の間は全件スキップ（安全側）。n8nクラウドはローカルCSVを読めないため CSV→Codeノード転記運用（手順は design/RIO_700_SERIES_RUNBOOK.md に追記）。JSON構文・接続整合性・照合ロジックはローカルNode.jsシミュレーションで検証済み。**注意: n8nへは再インポートが必要。再インポート後は Anthropic / Bearer Auth の Credential 紐付けを必ず確認すること。** 未コミット（CEOがまとめてPR予定） | このセッション |
| 2026-08-03 | 楽天アフィリ投稿のPR表記短縮（ゆうさん指示「AI投稿と分かりやすいので付けないでほしい」→CEO判断で完全削除ではなく短縮表記に）。DEV_RIO_705の「Match Rakuten Link」ノードjsCode内 `pr_line` を `'【PR】' + (entry.pr_note ? ... : '楽天アフィリエイトリンクを含みます')` から固定文字列 `'#PR'` に変更。`pr_note` フィールド自体はライブラリ側に運用メモとして残置（投稿文生成には未使用）。`meta.description` の【PR】表記もあわせて更新。`data/rakuten_link_library.csv` のカラム定義コメント（pr_note欄の説明、手順4のPR表記の記述）を実態に合わせて更新。`design/RIO_700_SERIES_RUNBOOK.md`（現在の運用手順を記述する現行ドキュメントのため、価格変更時と異なりこちらは履歴ではなく更新対象）の該当箇所も【PR】→#PRに修正し変更理由を追記。JSON構文検証（`python3 -m json.tool`）PASS、CSV列数検証（pythonのcsvモジュール、7列×5行）PASS。ローカルNode.jsシミュレーションを2段階で実施：(1) ロジック再実装での検証、(2) 修正後JSONから実際の jsCode 文字列を抽出しvmモジュールで直接実行（$input/$の最小限モック使用）— いずれも `final_reply` が `本文\n\n#PR\nURL` の形式になり、`【PR】`・「楽天アフィリエイトリンクを含みます」の文言が投稿文に一切含まれないことを確認。Link Gate / NO_LINK_SKIP / リンクライブラリ照合ロジックは無変更。**注意: n8nへ再インポート必要。再インポート後は Anthropic / Bearer Auth の Credential 紐付けを必ず確認。** 未コミット（CEOがまとめてn8n反映・PR作成） | このセッション |
| 2026-08-02 | 価格戦略変更（Brainキット¥3,980→¥1,980値下げ／口コミ返信テンプレ30本¥980を入口商品として新規公開）のリポジトリ内反映。`grep -rn "3980\|3,980"` で全箇所を洗い出し、「今後の作業判断に使う現在価格情報」のみ22ファイルを更新（REPORT.md・過去ログ・テスト用ダミー数値・design/REVENUE_SCALING_ROADMAP.mdの仮想シミュレーション例は履歴改ざん防止のため意図的に不変更）。`products/revenue-intelligence-os/data/offers.csv` の off_21 price を 3980→1980 に更新し、新規行 off_22（口コミ返信テンプレ30本・price=980・status=pending_publish・url="要確認（ゆうさんのBrain実サイト公開後に反映）"）を追加。同ディレクトリの design/*.md（DATA_MODEL, SYSTEM_MAP, PORTFOLIO_INVENTORY, REVENUE_LOOP_MVP, WIP_POLICY, RISK_APPROVAL_POLICY）とアフィリ報酬（50%=¥1,990→¥990）、`data/revenue_portfolio/portfolio_master.csv`、obsidian REVENUE_PORTFOLIO 配下13ファイル（00_DASHBOARD / 01_B2B_AGENT / 02_MICRO_SAAS / 03_PROGRAMMATIC_SEO / 04_SNS_AFFILIATE の README/OFFER/SALES_FLOW/KPI）を新価格に更新。CSV整合性はPythonのcsvモジュールでパース検証済み（列数崩れなし）。未コミット（CEOがまとめる） | このセッション |

---

## 他社員への申し送り

| 日付 | 宛先 | 内容 |
|---|---|---|
| 2026-08-02 | CEO/ゆうさん | 価格戦略変更のリポジトリ内反映が完了。ゆうさんがBrain実サイトで（1）キット価格を¥1,980に変更（2）口コミ返信テンプレ30本を¥980で新規公開、した後、`products/revenue-intelligence-os/data/offers.csv` の off_22 行の url（現在「要確認」のプレースホルダ）を実URLに差し替える必要あり。また `04_SNS_AFFILIATE/TASKS.md` の該当タスク（Brain部品商品A公開）完了チェックをお願いしたい |
